--- src/apu/Apu.java
+++ src/apu/Apu.java
@@ -236,7 +236,7 @@
         
         if (bufferIndex >= buffer.length) {
             if (line != null && line.isRunning()) {
-                line.write(buffer, 0, buffer.length);
+                if (line.available() >= buffer.length) line.write(buffer, 0, buffer.length);
             }
             bufferIndex = 0;
         }
