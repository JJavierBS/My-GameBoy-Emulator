--- src/apu/Apu.java
+++ src/apu/Apu.java
@@ -240,6 +240,9 @@
         if (bufferIndex >= buffer.length) {
+            if (Math.random() < 0.05) {
+                System.out.println("Buffer full. line=" + (line != null) + " isRunning=" + (line != null ? line.isRunning() : false));
+            }
             if (line != null && line.isRunning()) {
                 if (Math.random() < 0.5) {
                     System.out.println("Writing audio buffer");
                 }
