--- content/src/memory/Memory.java
+++ content/src/memory/Memory.java
@@ -62,6 +62,10 @@
 
 	public void writeByte(int address, byte data) {
 		address &= 0xFFFF; // Asegura que est? en rango
+		if (address == 0xFF47) {
+			System.out.println("Trace: write to 0xFF47 with value " + String.format("0x%02X", data));
+		}
+
 		if (address < 0x8000) {
 			// ROM: Normalmente es solo lectura, salvo para controlar el MBC
 			cartridge.write(address, data);
